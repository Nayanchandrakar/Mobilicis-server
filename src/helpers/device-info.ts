// import DeviceDetector from "device-detector-js";
// import type { DeviceDetectorResult } from "device-detector-js";

// Initialize DeviceDetector
// const detector = new DeviceDetector();

// Function to parse user agent and extract device information
// export function parseUserAgent(userAgent: string) {
//   const result: DeviceDetectorResult = detector?.parse(userAgent);

//   const { client, device, os } = result;

//   return {
//     deviceOS: os.name,
//     deviceType: device?.type,
//     deviceBrand: device?.brand || "Linux",
//     clientName: client?.name,
//   };
// }
